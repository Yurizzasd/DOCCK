# ambiente local (copie para .env e preencha)
VITE_API_URL=
VITE_SITE_URL=http://localhost:5173

# ---- Anúncios (AdCash AutoTag) ----
# VITE_ADS_PROVIDER=adcash
# VITE_ADS_ZONE=soidkao0ue
# Com as duas definidas, os slots passam a exibir anúncios sozinhos.
# Sem elas, aparecem placeholders neutros (layout preservado).
VITE_ADS_PROVIDER=
VITE_ADS_ZONE=
