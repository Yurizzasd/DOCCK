# ambiente local
VITE_API_URL=
VITE_SITE_URL=http://localhost:5173

# ---- Anúncios ----
# Provedor atual: 'adcash'. No futuro, troque para 'adsense' e ajuste a tag.
# Deixe vazio/off para exibir placeholders (layout preservado, sem anúncios).
VITE_ADS_PROVIDER=
# URL exata do <script> fornecido pelo painel do provedor (ex.: tag do banner AdCash)
VITE_ADS_SCRIPT=
# Zone / ad-slot id da unidade criada no painel do provedor (quando houver)
VITE_ADS_ZONE=
